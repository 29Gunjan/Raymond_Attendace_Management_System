{
  "name": "raymond-attendance-monorepo",
  "version": "1.0.0",
  "description": "Monorepo - Deploy backend/ and frontend/ as separate Railway services",
  "private": true,
  "scripts": {
    "start": "echo 'ERROR: Set Root Directory to backend/ or frontend/ in Railway Settings' && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

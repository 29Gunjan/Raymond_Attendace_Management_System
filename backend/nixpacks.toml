# Nixpacks configuration for Railway
# This tells Railway to use Node.js and how to build

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Backend ready'"]

[start]
cmd = "node src/server.js"
